from setuptools import setup, find_packages

setup(
    name="recipe-api",
    description="setup.py for recipe-api",
    version="v2023.08.26",
    packages=find_packages(include=["recipe_api"]),
    author="Noah Ward",
    install_requires=[],
)
